#include<iostream>

using namespace std;


box.mTranform = mTranslate *mPlayerRotatioin * mPlayerScaling;



 prop1.mTransform  = mTranslate * mRotate * mScaling;
 prop1.aabbSize.vecMin = vecPropMin;
 prop1.aabbSize.vecMax =  vecPropMax;

 mScaling.SerScale(Vector(1,2,3));
 mRotation.SetRotation(30,Vector(0,1,0));
 mTranlation.SetTranslation(Vector(10,0,15));

